import styled from "styled-components";

export const Button = styled.div`
  position: fixed;
  right: 0.75rem;
  bottom: 3rem;
  height: 1rem;
  font-size: 3rem;
  z-index: 1;
  cursor: pointer;
  color: white;
`;
